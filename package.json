{
  "name": "stripe-payments-demo",
  "version": "1.0.0",
  "description": "Stripe payments demo",
  "scripts": {
    "sync-snippets": "node scripts/sync-snippets.js",
    "client": "node scripts/sync-snippets.js && cd client && npm start",
    "server": "cd server && node index.js"
  }
}
